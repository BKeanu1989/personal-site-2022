# wtf is happening
vue 3 compiler:
    rollupOptions: {
      external: [
        // './@vue/composition-api/dist/vue-composition-api.mjs'
      ]
    }

    ... in vite config
    not workingin project - had to delete eslint again